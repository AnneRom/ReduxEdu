import { LoginForm } from "../../LoginForm/LoginForm";

const LoginPage = () => {
    return (
        <div>
            <h1>Сторінка входу</h1>
            <LoginForm />
        </div>
    );
};

export default LoginPage;